name: Native Edge Node N0

on:
  push:
    branches:
      - feature/native-edge-node-research
    paths:
      - "android/edge-node-native/**"
      - ".github/workflows/native-edge-node-n0.yml"
  pull_request:
    paths:
      - "android/edge-node-native/**"
      - ".github/workflows/native-edge-node-n0.yml"

jobs:
  build-n0:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android components
        run: |
          sdkmanager "platforms;android-35" "build-tools;35.0.0" "ndk;27.0.12077973" "cmake;3.22.1"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Build N0 debug APK
        working-directory: android/edge-node-native
        run: gradle :app:assembleDebug --stacktrace

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: orbi-edge-node-n0-debug
          path: android/edge-node-native/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
