name: Native Alpha Performance Fix

on:
  push:
    branches:
      - feature/native-alpha-perf-fix
    paths:
      - "android/edge-node-native/**"
      - ".github/workflows/native-alpha-perf-fix.yml"
  pull_request:
    paths:
      - "android/edge-node-native/**"
      - ".github/workflows/native-alpha-perf-fix.yml"

jobs:
  build-optimized-alpha:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android components
        run: |
          sdkmanager "platforms;android-35" "build-tools;35.0.0" "ndk;27.0.12077973" "cmake;3.22.1"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Run Android unit tests
        working-directory: android/edge-node-native
        run: gradle :app:testDebugUnitTest --stacktrace

      - name: Build optimized installable APK
        working-directory: android/edge-node-native
        run: gradle :app:assembleRelease --stacktrace --info

      - name: Verify optimized APK
        run: |
          APK="android/edge-node-native/app/build/outputs/apk/release/app-release.apk"
          test -f "$APK"
          unzip -l "$APK" | grep -q "lib/arm64-v8a/liborbi_native.so"
          sha256sum "$APK" | tee android/edge-node-native/app/build/outputs/apk/release/app-release.apk.sha256

      - name: Upload optimized APK
        uses: actions/upload-artifact@v4
        with:
          name: orbi-edge-node-native-alpha-perffix
          path: |
            android/edge-node-native/app/build/outputs/apk/release/app-release.apk
            android/edge-node-native/app/build/outputs/apk/release/app-release.apk.sha256
          if-no-files-found: error
